(library
 (name commands)
 (libraries utils core language synthesizer parser classify ocaml_parser evaluation zquickcheck analysis ifc)
 (modules
   caux
   ctest
   csyn
   cparse
   cev
   cbaseline
   ctime
 )
 (preprocess (pps ppx_jane))
)
